<% provide(:title, "Edit Business") %>

<div class="container mt-3 w-50 mx-auto">
    <%= link_to sanitize("&larr; Back to #{@business.name}"), @business %>
    <div class="header my-4">
        <h1 class="h4">
            Business Settings
        </h1>
        <p>Edit <%= @business.name %></p>
    </div>

    <div class="wrapper py-4 px-3 bg-white border border-light-emphasis">
        <%= render 'form', business: @business %>
    </div>
</div>
