<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Lato&family=Open+Sans:ital@0;1&display=swap" rel="stylesheet">
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%#= javascript_include_tag 'custom/sidebar.js' %>
  </head>

  <body style="margin-top: 62px">
    <div class="main-container d-flex">
      <div class="h-100">
        <%= render 'layouts/sidebar' %>
      </div>

      <div style="width: 100vw;">
        <%= render 'layouts/navbar' %>

        <div id="flash" class="flash">
          <%= render "layouts/flash" %>
        </div>
        
        <div class="fixed-sidebar-margin">
          <%= yield %>
        </div>
      </div>
  </body>
</html>
